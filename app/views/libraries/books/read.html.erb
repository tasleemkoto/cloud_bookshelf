<div class="container-fluid py-4">
<%= link_to "Back to Book", library_book_path(@library, @book), class: 'btn btn-secondary mb-4' %>
  <iframe src="<%= pdf_url %>" width="100%" height="590px" frameborder="0">
    Your browser does not support PDFs. Please download the PDF to view it:
    <a href="<%= pdf_url %>">Download PDF</a>
  </iframe>
</div>
